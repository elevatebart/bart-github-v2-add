describe("empty spec", () => {
  it("passes", () => {
    // throw Error("arbitrary error");
    cy.visit("https://example.cypress.io");
  });

  it("passes too", () => {
    // throw Error("arbitrary error");
    cy.visit("https://example.cypress.io");
  });
});
